---
import { content } from "../content/data";
import Layout from "../layouts/Layout.astro";
---

<Layout
  title="Gallery — Mohammadamin Kafi"
  description="A visual gallery of generated atmospheric studies for Mohammadamin Kafi's three-world resume website."
>
  <header class="page-heading">
    <p class="eyebrow">Visual studies</p>
    <h1>Atmospheres before worlds.</h1>
    <p>
      Three abstract color and light studies that preceded the Garden,
      Cathedral, and After Tomorrow art directions. These are generated
      decorative works, not documentary photographs.
    </p>
  </header>
  <div class="gallery-grid">
    {
      content.gallery.map((item) => (
        <figure class="gallery-card">
          <img
            src={item.src}
            alt={item.alt}
            width={item.width}
            height={item.height}
            loading="lazy"
          />
          <figcaption>
            <span>{item.caption}</span>
            {item.generated && <small>Generated artwork</small>}
          </figcaption>
        </figure>
      ))
    }
  </div>
</Layout>
